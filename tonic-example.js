// Well, You can't present a form without a document, but as a POC the object should have an `installed:true` property, indicating we are good to go

var Vue = require("vue")
var VueFormular = require("vue-formular")

Vue.use(VueFormular)
VueFormular
